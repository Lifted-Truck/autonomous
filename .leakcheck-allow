# leak_gate allowlist — paths exempt from the absolute-path scan because they
# legitimately contain the PATTERNS (documentation about leaks), not leaks.
# One path/glob per line; # comments ok. Exemptions are commits — a
# security-sensitive one should be justified in DECISIONS.md.
#
# Canonical security-sweep spec + runbook — contain rg/git-grep pattern
# examples (/Users/[A-Za-z]+, /Users/|/home/) that are docs, not leaks.
governor/REPO-HYGIENE.md
governor/HISTORY-REMEDIATION.md
# spectral-morph-001 (2026-08-17): a brief ABOUT the leak gate that quotes the
# escaped Windows form as an example of the self-match trap it is warning
# about. Visitor's words; the resident does not edit a brief body to satisfy
# a gate. Allowlisting the one file is the honest fix; the quoted form is a
# placeholder in intent, just not in the exempt spelling.
integrations/spectral-morph/brief.md
