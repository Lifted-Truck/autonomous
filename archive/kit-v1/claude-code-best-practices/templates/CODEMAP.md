# {{PROJECT_NAME}} — code map

A lightweight table of contents. Especially valuable when the directory layout is
unconventional. Keep entries to one line; link to the subdir CLAUDE.md for detail.

## Top-level structure

| Path | What lives here | Local CLAUDE.md? |
|------|-----------------|------------------|
| `/` | Project root, build config | — |
| `src/` | _main source_ | _add one_ |
| `tests/` | _test suite_ | — |
| `docs/` | _documentation_ | — |

> Replace the rows above with the real layout. Add a row per major area. Mark which
> areas have their own `CLAUDE.md`.

## Entry points

- _Where execution starts (e.g. `src/main.ts`, `cmd/server/main.go`)._

## Where to look for X

- **Config:** _path_
- **Data models / schema:** _path_
- **Public API surface:** _path_
- **Generated code (don't hand-edit):** _path_

## Glossary (optional)

- _Project-specific term_ — _what it means._
